const state = {
  products: []
}
const getters = {}
const mutations = {
  setProducts (state, payload) {
    stat
  }
}
const actions = {}

export default {
  namespaced: true,
  state,
  getters,
  mutations,
  actions
}
