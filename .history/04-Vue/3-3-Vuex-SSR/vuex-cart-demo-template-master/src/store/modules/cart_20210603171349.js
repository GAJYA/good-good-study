const state = {

}
const getters = {}
const mutations = {
  addToCart (state, product) {
    // 1.cartP
  }
}
const actions = {}

export default {
  namespaced: true,
  state,
  getters,
  mutations,
  actions
}
