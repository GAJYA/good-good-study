// es中可以导入commonjs模块

// import common from './common.js'

// 不能直接提取成员，注意，import不是解构导出的对象
// import { foo } from './common.js'
// const { foo, bar } = common
// console.log(common)
// console.log(foo, bar)

export const foo = 'is it already?'
