const state = {

}
const getters = {}
const mutations = {
  addToCart (state, product) {
    // 1.cartProducts中没有该商品，把该商品
  }
}
const actions = {}

export default {
  namespaced: true,
  state,
  getters,
  mutations,
  actions
}
